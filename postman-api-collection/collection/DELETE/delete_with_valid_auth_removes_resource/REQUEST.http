### API-DELETE-006: Delete With Valid Auth Removes Resource
# Run after Setup: Create User (or POST create). user_id must be a resource you own.
DELETE {{base_url}}/users/{{user_id}}
Authorization: Bearer {{auth_token}}
