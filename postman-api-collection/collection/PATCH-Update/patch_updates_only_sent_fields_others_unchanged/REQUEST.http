### API-PATCH-002: Patch Updates Only Sent Fields
# Run after Setup. user_id must be a resource you own.
PATCH {{base_url}}/users/{{user_id}}
Authorization: Bearer {{auth_token}}
Content-Type: application/json

{"status":"active"}
