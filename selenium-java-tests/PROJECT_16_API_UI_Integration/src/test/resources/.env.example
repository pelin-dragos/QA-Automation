# PROJECT_16 — API + UI Integration
# Copy to .env or set in environment. No secrets; base URL only.

# API (and UI) base URL (default: JSONPlaceholder)
# API_BASE_URL=https://jsonplaceholder.typicode.com/
